# tsmend — moved

`@shipispec/tsmend` has been folded into **[`@shipispec/tsfix`](https://github.com/owgreen-dev/tsfix)**. This repo is archived.

```bash
npm install @shipispec/tsfix
```

```ts
import { runMendLoop, mendSingleFile, getTypeContext } from "@shipispec/tsfix";
```

Full migration notes (where each file moved, why the consolidation happened): see [`MOVED.md`](./MOVED.md).

All 9 commits of this repo's history are preserved inside tsfix via `git merge --allow-unrelated-histories` — browsable in [tsfix's commit log](https://github.com/owgreen-dev/tsfix/commits/main).
